'use strict';

var should = require('should');
var onMicChange = require('../../util/onMicChange');

describe('onMicChange()', function() {
  it.skip('should call the callback on volume change', function(done) {

  });
  it.skip('should stop listening when end is called', function(done) {

  });
});
